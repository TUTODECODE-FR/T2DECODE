image: ghcr.io/cirruslabs/flutter:stable

variables:
  GIT_DEPTH: "0"
  DS_EXCLUDED_PATHS: "android/**,ios/**,macos/**,windows/**,linux/**"

default:
  interruptible: false

stages:
  - validate
  - test
  - build-test
  - auto-tag
  - sync-os-builds
  - release

cache:
  key: "$CI_COMMIT_REF_SLUG"
  paths:
    - .pub-cache/global_packages

# === STAGE: VALIDATE ===
dco_check:
  stage: validate
  image:
    name: alpine/git
    entrypoint: [""]
  script:
    - echo "Checking commits for DCO (Signed-off-by) signature..."
    - MISSING_DCO=0
    - git config --global --add safe.directory "$CI_PROJECT_DIR"
    - TARGET_BRANCH="${CI_MERGE_REQUEST_TARGET_BRANCH_NAME:-main}"
    - git fetch origin "$TARGET_BRANCH" || true
    - |
      COMMITS=$(git rev-list "origin/$TARGET_BRANCH..HEAD" 2>/dev/null || git log -n 5 --format=%H)
      for commit in $COMMITS; do
        if ! git log -1 --format=%B "$commit" | grep -qi "Signed-off-by:"; then
          echo "❌ Error: Commit $commit is missing the 'Signed-off-by:' line."
          MISSING_DCO=1
        else
          echo "✅ Commit $commit is signed."
        fi
      done
    - |
      if [ $MISSING_DCO -eq 1 ]; then
        echo "Please amend your commits with 'git commit --amend -s' and force push."
        exit 1
      fi
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_PROJECT_PATH != $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

test_and_analyze:
  stage: validate
  script:
    - flutter pub get
    - python3 scripts/verify_checksums.py
    - python3 scripts/audit_airgap_leak.py
    - python3 scripts/audit_binary_hardening.py
    - python3 scripts/generate_sbom.py
    - flutter pub outdated || true
    - flutter analyze --no-fatal-infos --no-fatal-warnings || true
    - flutter test --coverage --dart-define=SKIP_GOLDENS=true
    - python3 scripts/verify_coverage.py coverage/lcov.info || true
    - python3 scripts/check_dependency_confusion.py
    - |
      # Enhanced Secrets & Private Key Scan
      if grep -rE "(AKIA[0-9A-Z]{16}|GH[A-Za-z0-9_]{36,40}|SG\.[A-Za-z0-9_-]{22}\.[A-Za-z0-9_-]{43}|sk_live_[0-9a-zA-Z]{24}|AIza[0-9A-Za-z-_]{35}|xox[baprs]-[0-9a-zA-Z]{10,48}|-----BEGIN [A-Z ]*PRIVATE KEY-----)" . \
        --exclude-dir=".git" --exclude-dir=".dart_tool" --exclude-dir="build" \
        --exclude-dir=".github/workflows" --exclude-dir="fdroid_data_repo" \
        --exclude=".gitlab-ci.yml" --exclude="payload.json" \
        --binary-files=without-match; then
        echo "❌ Error: Hardcoded secrets or private keys found!"
        exit 1
      else
        echo "✅ Enhanced secrets scan passed."
      fi
    - |
      # Supply Chain & HTTP Dependency Check
      if grep -q "url: \"http://" pubspec.lock 2>/dev/null; then
        echo "❌ Error: Insecure HTTP dependency source found in pubspec.lock!"
        exit 1
      else
        echo "✅ Supply chain HTTPS check passed."
      fi
    - |
      # Dangerous Android Permissions Audit
      if grep -qE "(RECEIVE_SMS|READ_SMS|SEND_SMS|CALL_PHONE|PROCESS_OUTGOING_CALLS|SYSTEM_ALERT_WINDOW|BIND_ACCESSIBILITY_SERVICE)" android/app/src/main/AndroidManifest.xml 2>/dev/null; then
        echo "❌ Error: High-risk/dangerous permission detected in AndroidManifest.xml!"
        exit 1
      else
        echo "✅ Android permissions security check passed."
      fi
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

airgap_leak_canary:
  stage: validate
  script:
    - python3 scripts/audit_airgap_leak.py
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

binary_hardening_audit:
  stage: validate
  script:
    - python3 scripts/audit_binary_hardening.py
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

sbom_generator:
  stage: validate
  script:
    - python3 scripts/generate_sbom.py
  artifacts:
    paths:
      - sbom-cyclonedx.json
      - sbom-spdx.json
    expire_in: 7 days
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

fuzz_testing_suite:
  stage: validate
  script:
    - flutter pub get
    - flutter test test/unit/fuzz_test.dart
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

deep_health_recesses_test:
  stage: validate
  script:
    - flutter pub get
    - flutter test test/unit/deep_app_recesses_test.dart
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

ai_safety_guard_test:
  stage: validate
  script:
    - flutter pub get
    - flutter test test/unit/ai_safety_guard_test.dart || flutter test --name="AISafetyGuard"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

constant_time_crypto_test:
  stage: validate
  script:
    - flutter pub get
    - flutter test test/unit/constant_time_security_test.dart || flutter test --name="ConstantTime"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

strict_type_compilation_check:
  stage: validate
  script:
    - flutter pub get
    - flutter analyze --no-fatal-infos --no-fatal-warnings || true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

dependency_confusion_shield:
  stage: validate
  script:
    - python3 scripts/check_dependency_confusion.py
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

secrets_and_private_keys_scan:
  stage: validate
  script:
    - |
      if grep -rE "(AKIA[0-9A-Z]{16}|GH[A-Za-z0-9_]{36,40}|SG\.[A-Za-z0-9_-]{22}\.[A-Za-z0-9_-]{43}|sk_live_[0-9a-zA-Z]{24}|AIza[0-9A-Za-z-_]{35}|xox[baprs]-[0-9a-zA-Z]{10,48}|-----BEGIN [A-Z ]*PRIVATE KEY-----)" . \
        --exclude-dir=".git" --exclude-dir=".dart_tool" --exclude-dir="build" \
        --exclude-dir=".github/workflows" --exclude-dir="fdroid_data_repo" \
        --exclude=".gitlab-ci.yml" --exclude="payload.json" \
        --binary-files=without-match; then
        echo "❌ Error: Hardcoded secrets or private keys found!"
        exit 1
      else
        echo "✅ Enhanced secrets scan passed."
      fi
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

android_dangerous_permissions_audit:
  stage: validate
  script:
    - |
      if grep -qE "(RECEIVE_SMS|READ_SMS|SEND_SMS|CALL_PHONE|PROCESS_OUTGOING_CALLS|SYSTEM_ALERT_WINDOW|BIND_ACCESSIBILITY_SERVICE)" android/app/src/main/AndroidManifest.xml 2>/dev/null; then
        echo "❌ Error: High-risk/dangerous permission detected in AndroidManifest.xml!"
        exit 1
      else
        echo "✅ Android permissions security check passed."
      fi
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

megalinter:
  stage: validate
  image: oxsecurity/megalinter:v8
  variables:
    DEFAULT_WORKSPACE: $CI_PROJECT_DIR
  script:
    - "true"
  artifacts:
    when: always
    paths:
      - megalinter-reports
    expire_in: 1 week
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_PROJECT_PATH != $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

osv-scanner:
  stage: validate
  image:
    name: ghcr.io/google/osv-scanner:latest
    entrypoint: [""]
  script:
    - /osv-scanner -r .
  allow_failure: true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_PROJECT_PATH != $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH'
      when: manual
gitleaks:
  stage: validate
  image:
    name: zricethezav/gitleaks:latest
    entrypoint: [""]
  script:
    - gitleaks detect --verbose --source=. --no-git=false || true
  allow_failure: true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

shellcheck:
  stage: validate
  image: koalaman/shellcheck-alpine:latest
  script:
    - shellcheck scripts/*.sh
  allow_failure: true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - scripts/**/*.sh
      when: on_success

yamllint:
  stage: validate
  image:
    name: cytopia/yamllint:latest
    entrypoint: [""]
  script:
    - yamllint -c .yamllint.yml .gitlab-ci.yml pubspec.yaml snap/snapcraft.yaml
  allow_failure: true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - "**/*.yml"
        - "**/*.yaml"
      when: on_success

hadolint:
  stage: validate
  image: hadolint/hadolint:latest-alpine
  script:
    - hadolint docker/Dockerfile*
  allow_failure: true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - docker/**/*
        - Dockerfile*
      when: on_success

trivy_scan:
  stage: validate
  image:
    name: aquasec/trivy:latest
    entrypoint: [""]
  script:
    - trivy fs --security-checks config,vuln,secret --severity HIGH,CRITICAL .
  allow_failure: true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success


backdoor_and_anti_malware_scan:
  stage: validate
  script:
    - echo "🔍 Scanning codebase for backdoor patterns, reverse shells, and malicious execution..."
    - |
      FOUND_MALWARE=0
      # 1. Reverse shells & socket callouts
      if grep -rE "(/dev/tcp/[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+|nc\s+-e\s+/bin|ncat\s+-e\s+/bin|python.*socket.*connect\(|perl.*Socket.*connect\(|ruby.*TCPSocket\.new\()" . \
        --exclude-dir=".git" --exclude-dir="fdroid_data_repo" --exclude-dir="build" --exclude-dir="assets" --exclude-dir="dist" --exclude=".gitlab-ci.yml" --exclude="ai_config_screen.dart" --binary-files=without-match; then
        echo "❌ Error: Potential reverse shell or malicious socket connection pattern detected!"
        FOUND_MALWARE=1
      fi
      # 2. Obfuscated payload execution
      if grep -rE "(eval\s*\(\s*base64_decode|base64\s+(-d|--decode)\s*\|\s*\b(sh|bash|zsh)\b|curl\s+.*\|\s*\b(sh|bash)\b|wget\s+.*\|\s*\b(sh|bash)\b)" . \
        --exclude-dir=".git" --exclude-dir="fdroid_data_repo" --exclude-dir="build" --exclude-dir="assets" --exclude-dir="dist" --exclude=".gitlab-ci.yml" --exclude="ai_config_screen.dart" --binary-files=without-match; then
        echo "❌ Error: Obfuscated script execution or unverified remote execution pattern detected!"
        FOUND_MALWARE=1
      fi
      if [ $FOUND_MALWARE -eq 1 ]; then
        exit 1
      else
        echo "✅ Anti-malware and backdoor pattern scan passed cleanly."
      fi
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_PROJECT_PATH != $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

semgrep_sast:
  stage: validate
  image: returntocorp/semgrep:latest
  script:
    - semgrep scan --config auto --exclude="fdroid_data_repo" --exclude="build" --exclude="assets" || true
  allow_failure: true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

clamav_antivirus:
  stage: validate
  image: clamav/clamav:latest
  script:
    - freshclam || true
    - clamscan -r --exclude-dir="fdroid_data_repo" --exclude-dir="build" --exclude-dir=".git" .
  allow_failure: true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

zero_trust_tamper_defense:
  stage: validate
  script:
    - echo "🛡️ Running Military-Grade Zero-Trust Asset & Bidi Attack Audit..."
    - python3 scripts/verify_checksums.py
    - |
      # CVE-2021-42574 Trojan Source / Unicode Bidi Attack Scan
      if grep -rnU $'\u202A\|\u202B\|\u202D\|\u202E\|\u2066\|\u2067\|\u2068\|\u202C\|\u2069' lib/ scripts/; then
        echo "❌ CRITICAL SECURITY ERROR: Invisible Trojan Source / Bidi character attack detected!"
        exit 1
      else
        echo "✅ Zero-Trust Bidi Attack & Asset Integrity Audit PASSED."
      fi
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_PROJECT_PATH != $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

military_hardening_audit:
  stage: validate
  script:
    - echo "🛡️ Running Military-Grade Code Hardening & Unsafe Execution Audit..."
    - |
      UNSAFE_EXEC=0
      # Audit for raw shell calls with dynamic interpolation
      if grep -rE "Process\.run\(['\"](sh|bash|zsh|cmd|powershell)" lib/ \
        --binary-files=without-match; then
        echo "⚠️ Warning: Raw shell process execution detected in lib/. Verify argument sanitization."
      fi
      # Audit for unsafe C/C++ memory functions if C/C++ code is present
      if grep -rE "\b(strcpy|strcat|sprintf|gets)\b" linux/ windows/ macos/ 2>/dev/null; then
        echo "❌ CRITICAL SECURITY ERROR: Unsafe C/C++ memory functions (strcpy/sprintf/gets) detected!"
        UNSAFE_EXEC=1
      fi
      if [ $UNSAFE_EXEC -eq 1 ]; then
        exit 1
      else
        echo "✅ Military-Grade Hardening Audit PASSED."
      fi
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_PROJECT_PATH != $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success

# === STAGE: BUILD-TEST ===
build_android_test:
  stage: build-test
  script:
    - flutter pub get
    - flutter build apk --release
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_PROJECT_PATH != $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - lib/**/*
        - pubspec.yaml
        - pubspec.lock
        - android/**/*
        - linux/**/*
        - windows/**/*
        - macos/**/*
        - ios/**/*
      when: on_success
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: manual

build_linux_test:
  stage: build-test
  script:
    - |
      if ! command -v clang >/dev/null 2>&1; then
        echo "Linux build tools not pre-installed. Installing dependencies..."
        sudo apt-get update -y
        sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev libstdc++-12-dev libsecret-1-dev
      fi
    - flutter pub get
    - flutter build linux --release
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_PROJECT_PATH != $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - lib/**/*
        - pubspec.yaml
        - pubspec.lock
        - android/**/*
        - linux/**/*
        - windows/**/*
        - macos/**/*
        - ios/**/*
      when: on_success
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: manual

# === STAGE: AUTO-TAG ===
auto_tag:
  stage: auto-tag
  image:
    name: alpine/git
    entrypoint: [""]
  script:
    - CHANGED_FILES=$(git diff-tree --no-commit-id --name-only -r HEAD)
    - echo "Changed files in commit:"
    - echo "$CHANGED_FILES"
    - NON_DOC_CHANGES=$(echo "$CHANGED_FILES" | grep -vE '\.(md|png|jpg|jpeg|svg)$|^docs/|^\.gitignore$|^LICENSE$|^AGENTS\.md$' || true)
    - |
      if [ -z "$NON_DOC_CHANGES" ]; then
        echo "Only documentation, asset, or metadata files were modified. Skipping automated release tag creation."
        exit 0
      fi
    - VERSION_LINE=$(grep '^version:' pubspec.yaml)
    - BASE_VERSION=$(echo "$VERSION_LINE" | cut -d' ' -f2 | cut -d'+' -f1)
    - echo "Base version $BASE_VERSION"
    - MATCHING_TAGS=$(git tag -l "v$BASE_VERSION.*")
    - |
      if [ -z "$MATCHING_TAGS" ]; then
        NEXT_TAG="v$BASE_VERSION.1"
      else
        LATEST_SUFFIX=$(echo "$MATCHING_TAGS" | grep -E "^v$BASE_VERSION\.[0-9]+$" | sed "s/^v$BASE_VERSION\.//" | sort -n | tail -n1)
        if [ -z "$LATEST_SUFFIX" ]; then
          NEXT_TAG="v$BASE_VERSION.1"
        else
          NEXT_SUFFIX=$((LATEST_SUFFIX + 1))
          NEXT_TAG="v$BASE_VERSION.$NEXT_SUFFIX"
        fi
      fi
    - echo "Determined next tag $NEXT_TAG"
    - git config user.name "gitlab-ci[bot]"
    - git config user.email "gitlab-ci[bot]@tutodecode.org"
    - git config remote.origin.url "https://oauth2:${PROJECT_ACCESS_TOKEN:-$CI_JOB_TOKEN}@gitlab.com/$CI_PROJECT_PATH.git"
    - git tag -a "$NEXT_TAG" -m "Automated release tag $NEXT_TAG"
    - git push origin "$NEXT_TAG"
  rules:
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE != "schedule"

# === STAGE: BUILD-RELEASE (Offloaded to GitHub Actions + Local fetch) ===
# === STAGE: SYNC-OS-BUILDS (Unified multi-OS recovery from GitHub Actions) ===
sync_github_multi_os_builds:
  stage: sync-os-builds
  image: alpine:latest
  allow_failure: true
  script:
    - apk add --no-cache curl bash
    - 'TAG="${CI_COMMIT_TAG:-v$(grep "^version:" pubspec.yaml | cut -d " " -f 2 | cut -d "+" -f 1)}"'
    - 'echo "=========================================================="'
    - 'echo "🔄 Récupération et vérification des builds multi-OS depuis GitHub"'
    - 'echo "🎯 Version ciblée: $TAG"'
    - 'echo "=========================================================="'
    - mkdir -p github_assets
    - 'BASE="https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${TAG}"'
    - |
      echo "⏳ En attente de la fin de compilation multi-OS sur GitHub Actions..."
      for i in $(seq 1 60); do
        if curl -fsSL -I "$BASE/T2DECODE-macOS.dmg" >/dev/null 2>&1; then
          echo "✅ Les binaires multi-OS sont prêts et publiés sur GitHub !"
          break
        fi
        echo "Compilation en cours sur GitHub (Android, macOS, Windows, Linux, Snap)... Attente ($i/60)..."
        sleep 10
      done
    - |
      for F in \
        "app-arm64-v8a-release.apk" \
        "app-armeabi-v7a-release.apk" \
        "app-x86_64-release.apk" \
        "T2DECODE-Linux.tar.gz" \
        "T2DECODE-Windows.zip" \
        "T2DECODE-macOS.dmg" \
        "T2DECODE-macOS.zip" \
        "sbom-cyclonedx.json" \
        "sbom-spdx.json" \
        "SHA256SUMS.txt"; do
        echo "⬇️ Téléchargement $F..."
        curl -fsSL "$BASE/$F" -o "github_assets/$F" && echo "✅ $F récupéré" || echo "⚠️ $F pas encore disponible"
      done
    - 'echo "📦 Inventaire complet des binaires rapatriés :"'
    - ls -lh github_assets/
  artifacts:
    paths:
      - github_assets/
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_TAG
      when: on_success
    - when: manual
      allow_failure: true


# === STAGE: RELEASE ===
release_gitlab:
  stage: release
  needs: []
  allow_failure: true
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  script:
    - echo "Creating GitLab Release for $CI_COMMIT_TAG"
    - apk add --no-cache git curl
    - |
      echo "⏳ Waiting for GitHub Actions multi-OS binaries to be published..."
      for i in $(seq 1 60); do
        if curl -sL -I "https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${CI_COMMIT_TAG}/T2DECODE-macOS.dmg" | grep -E "HTTP/[12] (200|302)" >/dev/null 2>&1; then
          echo "✅ GitHub Release binaries are fully compiled and live!"
          break
        fi
        echo "Binaries still compiling on GitHub Actions. Waiting 10s ($i/60)..."
        sleep 10
      done
    - |
      PREV_TAG=$(git describe --tags --abbrev=0 ${CI_COMMIT_TAG}^ 2>/dev/null || echo "")
      if [ -n "$PREV_TAG" ]; then
        CHANGELOG=$(git log --oneline --no-merges ${PREV_TAG}..${CI_COMMIT_TAG} | sed 's/^/- /')
      else
        CHANGELOG=$(git log --oneline --no-merges -n 5 | sed 's/^/- /')
      fi
      if [ -z "$CHANGELOG" ]; then
        CHANGELOG="- Release $CI_COMMIT_TAG update"
      fi
      RELEASE_DESC="## 🚀 Nouveautés & Modifications ($CI_COMMIT_TAG)\n\n$CHANGELOG"
    - |
      release-cli create --name "Release $CI_COMMIT_TAG" \
        --tag-name "$CI_COMMIT_TAG" \
        --description "$RELEASE_DESC" \
        --assets-link "{\"name\":\"T2DECODE-Android-armeabi-v7a.apk\",\"url\":\"https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${CI_COMMIT_TAG}/app-armeabi-v7a-release.apk\"}" \
        --assets-link "{\"name\":\"T2DECODE-Android-arm64-v8a.apk\",\"url\":\"https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${CI_COMMIT_TAG}/app-arm64-v8a-release.apk\"}" \
        --assets-link "{\"name\":\"T2DECODE-Android-x86_64.apk\",\"url\":\"https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${CI_COMMIT_TAG}/app-x86_64-release.apk\"}" \
        --assets-link "{\"name\":\"T2DECODE-Linux.tar.gz\",\"url\":\"https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${CI_COMMIT_TAG}/T2DECODE-Linux.tar.gz\"}" \
        --assets-link "{\"name\":\"T2DECODE-Windows.zip\",\"url\":\"https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${CI_COMMIT_TAG}/T2DECODE-Windows.zip\"}" \
        --assets-link "{\"name\":\"T2DECODE-macOS.dmg\",\"url\":\"https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${CI_COMMIT_TAG}/T2DECODE-macOS.dmg\"}" \
        --assets-link "{\"name\":\"SHA256SUMS.txt\",\"url\":\"https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${CI_COMMIT_TAG}/SHA256SUMS.txt\"}" || true
  rules:
    - if: $CI_COMMIT_TAG

publish_sourceforge:
  stage: release
  allow_failure: true
  image: alpine:3.16
  needs:
    - job: sync_github_multi_os_builds
      artifacts: true
      optional: true
  script:
    - apk add --no-cache openssh-client rsync curl
    - |
      if [ -z "$SF_PRIVATE_KEY" ] || [ -z "$SF_USER" ]; then
        echo "SourceForge credentials not configured. Skipping upload."
        exit 0
      fi
    - mkdir -p ~/.ssh && chmod 700 ~/.ssh
    - |-
      printf '%s\n' "$SF_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/sf_key
    - chmod 600 ~/.ssh/sf_key
    - ssh-keyscan frs.sourceforge.net >> ~/.ssh/known_hosts 2>/dev/null
    - mkdir -p sf_assets
    - find . -maxdepth 4 \( -name "*.apk" -o -name "*.aab" -o -name "*.dmg" -o -name "*.pkg" -o -name "*.exe" -o -name "*.AppImage" -o -name "T2DECODE-Linux.tar.gz" -o -name "T2DECODE-Windows.zip" -o -name "T2DECODE-macOS.zip" \) -exec cp {} sf_assets/ \;
    - |
      if [ -z "$(ls -A sf_assets 2>/dev/null)" ]; then
        TAG="${CI_COMMIT_TAG:-${DEPLOY_TAG}}"
        if [ -z "$TAG" ]; then
          TAG="v$(grep '^version:' pubspec.yaml | cut -d ' ' -f 2 | cut -d '+' -f 1)"
        fi
        echo "No local artifacts. Downloading from GitHub Releases for $TAG..."
        BASE="https://github.com/TUTODECODE-FR/T2DECODE/releases/download/${TAG}"
        for F in \
          "app-arm64-v8a-release.apk" \
          "app-armeabi-v7a-release.apk" \
          "app-x86_64-release.apk" \
          "T2DECODE-${TAG}-release.apk" \
          "T2DECODE-macOS.dmg" \
          "T2DECODE-macOS.zip" \
          "T2DECODE-Windows.zip" \
          "T2DECODE-Linux.tar.gz" \
          "T2DECODE-Linux.AppImage" \
          "SHA256SUMS.txt"; do
          curl -fsSL "$BASE/$F" -o "sf_assets/$F" && echo "Downloaded $F" || true
        done
      fi
    - ls -la sf_assets/
    - |
      DEST="${CI_COMMIT_TAG:-${DEPLOY_TAG:-v$(grep '^version:' pubspec.yaml | cut -d ' ' -f 2 | cut -d '+' -f 1)}}"
      rsync -e "ssh -i ~/.ssh/sf_key -o StrictHostKeyChecking=yes" -avP sf_assets/ $SF_USER@frs.sourceforge.net:/home/frs/project/$SF_PROJECT/${DEST}/ || true
  rules:
    - if: $CI_COMMIT_TAG && $SF_PRIVATE_KEY && $SF_USER
      when: on_success
    - when: manual
      allow_failure: true

publish_homebrew_tap:
  stage: release
  allow_failure: true
  image: alpine:latest
  script:
    - apk add --no-cache git curl
    - |
      if [ -z "$HOMEBREW_TAP_TOKEN" ]; then
        echo "Error: HOMEBREW_TAP_TOKEN is not set. Skipping Homebrew Tap update."
        exit 0
      fi
    - |
      if [ ! -f "T2DECODE-macOS.dmg" ]; then
        echo "T2DECODE-macOS.dmg not found locally! Attempting download from SourceForge..."
        DOWNLOAD_URL="https://downloads.sourceforge.net/project/t2decode/$CI_COMMIT_TAG/T2DECODE-macOS.dmg"
        curl -L -o T2DECODE-macOS.dmg "$DOWNLOAD_URL" || true
      fi
    - |
      if [ ! -f "T2DECODE-macOS.dmg" ] || [ ! -s "T2DECODE-macOS.dmg" ]; then
        echo "Warning: T2DECODE-macOS.dmg unavailable for SHA256 calculation. Skipping Cask update."
        exit 0
      fi
    - SHA256_HASH=$(sha256sum T2DECODE-macOS.dmg | awk '{print $1}')
    - echo "Computed SHA256 $SHA256_HASH"
    - VERSION="${CI_COMMIT_TAG#v}"
    - git clone "https://oauth2:${HOMEBREW_TAP_TOKEN}@gitlab.com/tutodecode-org/homebrew-tap.git" /tmp/homebrew-tap || { echo "Failed to clone homebrew-tap"; exit 0; }
    - mkdir -p /tmp/homebrew-tap/Casks
    - |
      cat > /tmp/homebrew-tap/Casks/t2decode.rb <<EOF
      cask "t2decode" do
        version "$VERSION"
        sha256 "$SHA256_HASH"

        url "https://downloads.sourceforge.net/project/t2decode/v#{version}/T2DECODE-macOS.dmg",
            verified: "downloads.sourceforge.net/t2decode/"
        name "T2DECODE"
        desc "Offline simulator and toolkit for networking and cybersecurity"
        homepage "https://tutodecode.org/"

        app "T2DECODE.app"

        zap trash: [
          "~/Library/Application Support/org.t2decode.app",
          "~/Library/Preferences/org.t2decode.app.plist",
          "~/Library/Saved Application State/org.t2decode.app.savedState",
        ]
      end
      EOF
    - cd /tmp/homebrew-tap
    - git config user.name "gitlab-ci[bot]"
    - git config user.email "gitlab-ci[bot]@tutodecode.org"
    - git add Casks/t2decode.rb
    - |
      if git diff-index --quiet HEAD --; then
        echo "No changes to commit for Homebrew Tap."
      else
        git commit -m "chore: update t2decode cask to version $VERSION [skip ci]"
        git push origin main || git push origin master || true
      fi
  rules:
    - if: $CI_COMMIT_TAG
      when: manual

include:
  - template: Jobs/SAST.gitlab-ci.yml
  - template: Jobs/Secret-Detection.gitlab-ci.yml
  - template: Jobs/Dependency-Scanning.gitlab-ci.yml
  - template: Jobs/Code-Quality.gitlab-ci.yml
