name: tdc_sdk
description: TDC SDK Studio — IDE dédié & Suite de développement pour le langage TUTODECODE Script (.tdc)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown_plus: ^1.0.7
  google_fonts: ^6.1.0
  provider: ^6.1.1
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
